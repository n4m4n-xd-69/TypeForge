node_modules/
dist/
dist-ssr/
.vite/
*.local
.DS_Store
.env
.env.*
!.env.example
# Forge AI Edge Function secrets. The template is committed; the filled-in
# file is not — it holds the four provider keys and is pushed to Supabase with
# `npm run forge:secrets`, never to git.
!supabase/.env.forge.example

# Credential notes — kept on disk for reference, never committed.
ai.md
database.md

# Vercel project linkage — machine-local, regenerated by `vercel link`.
.vercel/

# Supabase CLI scratch state — machine-local, regenerated by `supabase link`.
# The migrations in supabase/migrations/ ARE tracked; only this is not.
supabase/.temp/
.vercel
